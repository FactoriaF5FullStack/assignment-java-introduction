package org.factoriaf5;

public class Challenge {
    // ejemplo
    public static boolean isEven(int number) {
        boolean result = number % 2 == 0;
        return result;
    }

    public static int addTwoNumbers(int first, int second){
        int result = 0;
        return result;
    }

    // completar
    public static String greetingFor(String name) {
        String result = "";
        return result;
    }

    // completar
    public static boolean isOdd(int number) {
        boolean result = false;
        return result;
    }

    // completar
    public static int roundNumber(double number) {
        int result = 0;
        return result;
    }

    // completar
    public static String swapTwoNumbers() {
        /*
         * --Before swap--
         * First number = 1.20f
         * Second number = 2.45f
         *
         * --After swap--
         * First number = 2.45f
         * Second number = 1.20f
         */

        float first = 1.20f, second = 2.45f;

        // Your code here

        return "First number = " + first + " - Second number = " + second;
    }


    /**
     * Create a program to find the area of a rectangle.
     *
     * The formula to compute the area of a rectangle is "area = length * breadth"
     *
     * @param length
     * @param breadth
     * @return
     */
    public static int rectangleArea(int length, int breadth) {
        int area = 0;
        return area;
    }


    /*
     * Implement a function called kilometersToMiles that converts distance in kilometers to miles.
     * The formula to convert kilometers to miles is "miles = km / 1.6".
     *
     * 1. The function accepts a double parameter and assign it to the km variable.
     * 2. Use the above formula to convert the kilometer distance to miles.
     * 3. return the result
     */
    public static double kilometersToMiles(double km) {
        return 0;
    }
}